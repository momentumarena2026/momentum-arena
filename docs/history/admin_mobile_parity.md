---
name: admin-mobile-parity
description: "Full-parity mobile admin project — rebuild every web admin screen in the app; nav reorg + per-phase plan, build pattern, and progress"
metadata:
  type: project
  originSessionId: 5ab2a2c4-5e6a-4f62-9239-19e35bd79d9b
---

**Goal (user, 2026-06-27):** the mobile app must have the WHOLE web admin functionality — **full parity**, every web admin page rebuilt on mobile. Decided via AskUserQuestion: scope = "Full parity — everything"; nav = "Tabs + grouped More hub".

**Web admin = source of truth.** ~50 pages in `app/(admin)/admin/`, sidebar groups: General (overview/analytics×8/reports) · Bookings (list/unconfirmed/calendar/detail/create/edit/recovery/checkin/recurring) · Courts&Pricing (sports/equipment/pricing/slots/bowling) · Cafe (menu/orders/live/create/cafe-coupons) · Shop (products/orders/pos) · Promotions (coupons/rewards) · Operations (expenses+config) · Mobile-Apps (release-flow/ota/push) · Settings (users/groups/admin-users/generator/faqs/payment-settings/razorpay/profile) + not-in-sidebar (discounts-legacy/utr-verify). Detailed map saved at scratchpad `admin_map.md` during the session.

**Nav model (Phase 0, SHIPPED `032c2e0`):** bottom bar = **Home · Bookings · Calendar · Cafe · More** (5). Check-in/Expenses/Rewards stay registered as tabs but **hidden** (`tabBarButton:()=>null` + `tabBarItemStyle:{display:'none'}`), reachable from Dashboard quick-actions + the More hub. New `AdminDashboardScreen` (KPIs via `GET /api/mobile/admin/stats`), new `AdminMoreScreen` (grouped permission-gated hub; live rows navigate, unbuilt rows show "Soon" and flip to live as phases land — edit GROUPS in AdminMoreScreen.tsx). New `src/lib/admin-permissions.ts` (`adminCan(admin, perm)` mirrors web `lib/permissions.ts`, SUPERADMIN bypass; perm `null` = always). New More stack `AdminMoreStackParamList` — register Phase 1+ screens there so the hub can push.

**BUILD PATTERN (every screen):** new `/api/mobile/admin/*` route (the route lives in the WEB app so it CAN import web `lib/`, but web actions have their own NextAuth `requireAdmin` — so REPLICATE the web query inline: `getMobileAdmin(request)` → `hasPermission(admin.permissions, KEY)` w/ SUPERADMIN bypass → mirror the JSON; see `/api/mobile/admin/rewards/overview` as the canonical example) → client method in `src/lib/admin-*.ts` (use `request<T>(path,{method})`) → screen → add/flip its row in AdminMoreScreen GROUPS + register in the More stack. Cross-navigator `navigate` trips RN's typed overloads → use the `NavLike`/`go` cast pattern from AdminMoreScreen/AdminDashboardScreen. **No chart lib on mobile** — analytics hand-roll with `react-native-svg` (mirror AdminExpenseAnalyticsScreen).

**Phases = tasks #59–67.** #59 Phase 0 DONE (`032c2e0`). #60 Phase 1 DONE (`f89f9ad` cafe-coupons CRUD + `6f392c3` admin create-cafe-order; live board = existing AdminCafeOrders kanban, no new screen). #61 Phase 2 DONE (`4e2e63c` sports + `57145b3` equipment + `59ebfd3` bowling + `aaafd6d` pricing/arena-hours; pricing PEAK/OFF-PEAK band EDIT deferred to web — mobile shows bands read-only). **Patterns proven for reusing web actions: (a) add `adminOverride?:{id,username}` (like cancelCafeOrder) — used for adminCreateCafeOrder; (b) add `skipAuth=false` flag (like getAllSportsWithConfigs) — used for the 4 bowling actions; (c) just replicate small upserts/queries in the route (pricing). All under getMobileAdmin + hasPermission(SUPERADMIN bypass). More-stack screens now: AdminSports/Equipment/Bowling/Pricing/Coupons registered in AdminMoreStackParamList + AdminMoreStackNav, hub rows flipped live.** #62 Phase 3 DONE (`e4bad7c` unified coupons manager [More stack] + `2005e9d` rewards config [added skipAuth to getAdminRewardConfigFull/updateAdminRewardConfig; new AdminRewardsConfigScreen in the Rewards stack, tile on rewards home]). **User directive 2026-06-27: "continue all, don't ask on every next phase" — power through remaining phases, ship each slice to development, don't pause for confirmation.** **✅ FULL MOBILE ADMIN PARITY COMPLETE + ON MAIN (2026-06-28).** All 8 phases shipped via parallel-agent waves; every web admin sidebar entry now has a mobile screen reachable from the More hub (Dashboard + tabs Bookings/Calendar/Cafe/More; More hub mirrors the web sidebar groups, permission-gated). Last gaps closed: Reports (queue/track) + Slot-blocks surfaced in hub. Two UI bugs fixed: cafe cart drawer hidden behind bottom nav (z-[60]) + admin booking-detail collect form behind keyboard (Screen `keyboardVerticalOffset`=useHeaderHeight). **Promoted to main = `62734a3`** (clean [skip ci] merge, tree==development, schema-free, prod DB untouched; earlier admin merge was `1dc5b5d`). Reports XLSX download stays web-only (mobile can't save files w/o native module); OTA rollout stays web (read-only on mobile). Pattern for future admin screens: route (app/api/mobile/admin/<x>) + client (apps/mobile/src/lib/admin-<x>.ts) + screen + register in AdminMoreStack + flip hub row; reuse web actions via skipAuth/adminOverride param, else replicate query.

(historical) #63 Phase 4 Shop DONE (`22ee4bb` products + `a443c9b` orders+POS). #64 Phase 5 Engagement DONE (`a443c9b` push-send + OTA-readonly + release-flow). Phase 7 PARTIAL via `a443c9b` (users/user-groups/faqs/generator done; remaining payment-settings/razorpay/admin-users/profile). **PARALLEL AGENTS used (user-authorized): each agent builds ONLY its own NEW files (route+client+screen) + owns disjoint web-action files; orchestrator wires nav/types/AdminMoreScreen + typechecks + commits per wave. Wave 1 = 5 agents → 9 screens (a443c9b).** **✅ ALL PHASES 0–8 COMPLETE (2026-06-27).** Wave 2 (`82b0757`, 4 agents → 15 screens) finished #65 Analytics (sports/cafe/push/demand/cohorts/funnels/events; Reports stays web-only — async generator), #66 Settings (payment-settings/razorpay/admin-accounts-CRUD/profile; new actions/admin-admin-users.ts + admin-profile.ts), #67 Bookings-extras (recovery/recurring-config/utr-verify/legacy-discounts). Mobile admin now mirrors the full web sidebar (~30 screens in the More hub, permission-gated). Web tsc 0; mobile baseline 19 untouched. **Deferred to web (documented): Reports (async export); OTA rollout mutation (read-only on mobile); pricing PEAK/OFF-PEAK band editing; product image upload + category CRUD; coupon advanced targeting/conditions. **PROMOTED TO MAIN 2026-06-27 = `1dc5b5d`** (clean `--no-ff [skip ci]` merge, gate-verified tree-identical to development; schema-free so prod DB untouched). Included the 2 UI fixes (`b78a722`): web cafe action buttons clearing the bottom nav (/cafe/checkout added to bottom-nav HIDDEN_PATHS + cafe-menu cart bar lifted above the nav) + app admin booking-detail mark-collected keyboard avoidance (Screen avoidKeyboard + keyboardShouldPersistTaps + scroll-to-end on focus). Web live via Vercel; mobile reaches users via OTA rollout of the newest dev DRAFT.** Dev commits: 032c2e0, f89f9ad, 6f392c3, 4e2e63c, 57145b3, 59ebfd3, aaafd6d, e4bad7c, 2005e9d, 22ee4bb, a443c9b, 82b0757. (coupons/rewards-config), #63 Shop (products/orders/POS), #64 Engagement (push-send/OTA/release-flow), #65 Analytics (8 screens), #66 Settings (users/groups/admin-users/generator/faqs/payment-settings/razorpay/profile), #67 Bookings-extras (recovery/recurring-config/utr-verify/legacy-discounts). Ship each phase to `development` incrementally (no [skip ci]); promote to main when the user asks. Existing mobile admin clients already cover: bookings, cafe(items/orders/live/stats), calendar, checkin, expenses, rewards, slots.

**Web tsc must stay 0. Mobile tsc has ~19 PRE-EXISTING baseline errors** (admin-bookings/appVersion/push.ts/AccountScreen/AdminCalendarScreen/BookingsListScreen/ChatScreen/HomeScreen/Shop*) — keep new files clean; don't add to that count.
