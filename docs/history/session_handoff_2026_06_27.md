---
name: session-handoff-2026-06-27
description: "In-flight: push-notification testing — iOS delivery proven, foreground banner shipping via OTA; resume by rolling out the DRAFT + promoting to main"
metadata: 
  node_type: memory
  type: project
  originSessionId: 5ab2a2c4-5e6a-4f62-9239-19e35bd79d9b
---

**Where we are (push notifications).** The push stack was already fully built + wired (see [[project_booking_system]]); this session got iOS push working end-to-end and added a foreground banner. **iOS push delivery is PROVEN** on TestFlight build **29708669 (1.0.0)** — background/closed pushes arrive; the only gap was *foreground* (expected: iOS gives no system banner in foreground, FCM hands it to `onMessage`), now addressed.

**Creds — ALL in place (verified this session):** Firebase project `momentum-arena-11975`, app id `com.momentumarena`, iOS App Store id `6783955158`. (1) Push Notifications capability enabled on the App ID. (2) APNs Auth Key (.p8) uploaded to Firebase Cloud Messaging in BOTH the development + production slots (one .p8 covers both — it's environment-agnostic; certs left empty). (3) `FIREBASE_SERVICE_ACCOUNT_JSON` set on all three Vercel scopes (Production + Preview + Development); Preview powers `development.momentumarena.com` (what the dev build talks to).

**Shipped today:**
- **Version `bump` input** on native workflows + `bump`-on-canary docs — dev + **main** (see [[ota-self-hosted]]).
- **Production OTA canary auto-rollout** — dev + **main**.
- **iOS `aps-environment` → production** fix, commit **7e58be0** — **development ONLY, NOT yet on main.** (Our CI uses manual signing so Xcode never auto-swapped it; "development" silently dropped APNs pushes on TestFlight.) Verified working by actual delivery.
- **Foreground in-app banner**, commit **43f4506** — **development ONLY.** New `apps/mobile/src/components/InAppNotificationBanner.tsx` (slide-in, auto-dismiss, tap-routes) + `installForegroundMessageHandler` (onMessage) in `src/lib/push.ts` + RootNavigator wires it (refactored `dispatchPushTap` to a shared `useCallback`). Pure JS → ships via **OTA, no rebuild**.

**RESUME HERE TOMORROW (in order):**
1. **Roll out + test the foreground banner.** ota-publish run **28274212243** (sha 43f4506) **succeeded — the dev OTA DRAFT is created** (iOS + Android). Go to `development.momentumarena.com/admin/ota` → roll out the **iOS** DRAFT to 100% → **force-close + reopen the app TWICE** (Expo applies on the launch after download) → with the app **foregrounded**, broadcast from `/admin/push` → the in-app banner should slide in; tap → deep-links. (This doubles as the first real production-style OTA rollout test.)
2. **Promote to main once the banner is verified:** merge development → main to bring **7e58be0 (aps-environment)** + **43f4506 (banner)** onto main (main is at **25a9083**, has the bump-input + canary but NOT these two). Use the established clean-merge-with-`[skip ci]` approach (temp worktree on `origin/main`, `git merge --no-ff origin/development -m "... [skip ci]"`, push) so seed-production / the prod DB isn't touched. The aps-fix on main means future PROD iOS builds inherit production APNs.
3. **Roadmap (optional, discussed):** migrate foreground display to **Notifee** (`@notifee/react-native`, by the RN-Firebase team) for OS-style foreground banners + Android notification channels + local/scheduled notifications (e.g. on-device "slot starts in 1h"). Native module → next native build. Our custom banner is fine until then.

**Branch state at session end:** `origin/development` = **43f4506** (+ any ota-publish post-run baseline commits), `origin/main` = **25a9083**. dev is ahead of main by the aps-fix + banner (+ baseline commits).

**Update (2026-06-27, later) — more push work shipped to development, pending test+promote:** commits **6193ba9** (broadcast tap-destination picker: new "open_screen" PushKind + "On tap, open" dropdown on /admin/push → routes the tap to Home/Book/Cafe/Shop/Rewards; server+app+form) and **19a3652** (auto-apply OTA: App.tsx `useUpdates().isUpdatePending → reloadAsync()` so no second force-close — takes effect from the NEXT OTA after it's running; removed the Account "App Version" card + "Check for updates" button; footer text now `versionLabel()` with the 5-tap admin kept). Newest OTA DRAFT bundles foreground-banner + destination + auto-OTA + version-UI. Resume = roll out newest iOS DRAFT → test all four → then promote the whole push stack (aps-fix 7e58be0 + banner 43f4506 + destination 6193ba9 + auto-OTA/version-UI 19a3652) to main. **DONE 2026-06-27:** items 1-3 tested on device + working (account version UI, broadcast deep-link, foreground banner / background system banner); auto-OTA (#4) is inherently confirmed on the NEXT mobile OTA. **Whole push stack PROMOTED to main = `4824cef`** (tree == development; `[skip ci]` merge, prod DB untouched). iOS push notifications are now production-ready end-to-end.

**DONE 2026-06-27 (dev `54031b8`, main `ac44174`, [skip ci] merge; web-only, deploys via Vercel):** OTA dashboard guardrail — block rolling out a release OLDER than the currently-live one (hard-block). Rule: allow rollout only when target.createdAt ≥ live PUBLISHED release's createdAt (adjust-live-% and newer allowed; older blocked; no-live ⇒ all allowed). Enforce in `rolloutOtaRelease` (return error) + disable the button + tooltip in `app/(admin)/admin/ota/ota-matrix.tsx`. Rationale: rolling out an older release doesn't cleanly revert — manifest serves `createdAt: release.createdAt`, and expo-updates' selection policy refuses to load an older-createdAt update, so devices already on the newer build stay put → fragmented fleet. Legit revert paths (Rollback-to-embedded directive; publish-forward a new release) are untouched.

**Push Analytics dashboard + dispatch log — DONE 2026-06-27 (dev `d11bd91`, main `e51e4fb`; tree-identical).** New `/admin/analytics/push` ("Push" tab in the analytics section, gated VIEW_ANALYTICS). New `PushDispatch` Prisma model (one row per send: kind/scope/source/audience/sentByAdminId/title/body/attempted/succeeded/failed/cleanedUp/createdAt) — `lib/push.ts` logs every dispatch best-effort at the `sendToTokens` choke point via a `DispatchMeta` threaded from sendToUser/sendToAdmins; sendBroadcast + sendTestPushToUser pass source/audience/sentByAdminId (NOTE: `requireAdmin()` returns the admin OBJECT, use `.id`). Analytics in `actions/admin-push-analytics.ts` (getPushAnalytics + getDispatchedKinds): totals (sent/delivered/failed/dead-pruned/delivery-rate/broadcasts) + by-kind + by-source + sends-over-time + recent table; device fleet from PushDevice (reach/platform/active-stale/app-version/registrations). Dashboard `push-analytics-dashboard.tsx` (recharts, mirrors cafe-analytics pattern) with filters: date range, source, scope, kind(multi). Migration file `20260627010000_push_dispatch_log` + deploy `db push` created the table on staging + prod (PURELY ADDITIVE — verified diff = only the new table, so [skip ci] was NOT used on the main merge so seed-production's db push would create it; safe despite --accept-data-loss). Send metrics accrue from now; device metrics full history. Possible follow-ups offered but not built: per-platform delivery split, CSV export.

**Pre-production audit + fix marathon — ✅ COMPLETE 2026-06-27. All 16 fixes (#43–#58) shipped to `development`; pending user dev-test + ONE consolidated main promotion.** Dev commits in order: bc2b229 (#43/#47/#56), ac6754c (#45/#57), dbcbe94 (#46), 28b192a (#50), eef9fe3 (#48), 2106059 (#49), fb7a0ac (#44), 08eb7f7 (#51), 3fcda23 (#52), 52e315b (#53), 3760ede (#54), 7925782 (#55). #58 verified-no-change. **WHOLE BATCH IS SCHEMA-FREE** (#44 reused referredBy/referralCode; #51 reused deletedAt — both pre-existed in schema) → the final main merge CAN use `[skip ci]` (prod DB untouched). Web tsc = 0 errors; mobile tsc = 19 PRE-EXISTING baseline errors only (admin-bookings/appVersion/push.ts:249/AccountScreen x2/AdminCalendarScreen/BookingsListScreen/ChatScreen/HomeScreen/Shop* — none introduced by this batch; OTA pipeline tolerates them). **Deferred sub-items (documented in commits):** #52 mobile-cafe DQR (whole DQR dormant — gated + NPCI-blocked) + guest checkout (app is auth-required); #53 mobile Save-QR-to-gallery (needs a native capture/camera-roll module = rebuild not OTA; screen already guides screenshot). **Referral note:** the points-referral (referredBy + awardReferralBonus) is wired but OFF by default (config 0) and coexists with the live discount-code referral — no double-reward unless an admin opts in. **PROMOTED TO MAIN 2026-06-27 = `179f531`** (clean `--no-ff` `[skip ci]` merge of development→main; gate-verified tree-identical to development, prod DB untouched). Web fixes deploy via Vercel; mobile fixes reach users via OTA rollout (user rolls out the newest dev DRAFT). RESUME = roll out newest mobile OTA DRAFT to test on device; then production launch. **Play store listing + content rating = DONE (user, 2026-06-27).** Remaining launch work: Play — upload production AAB to a release track + roll out (Android push still UNTESTED, no device); iOS App Store — production build + review metadata + submit (must declare the new in-app **account deletion** in App Privacy / Data Safety since #51 added it — Apple + Play both require this).

**(historical) Pre-production audit + fix marathon — STARTED 2026-06-27.** Did a full mobile↔web code-parity audit (6 parallel agents); fixing ALL findings before production (user: all highest priority), shipping to `development` in batches, will promote to main once done + dev-tested. **Two product decisions locked:** (1) mobile gateway = RELABEL the tile to "Pay Online" + keep Razorpay (don't build PhonePe-on-mobile); (2) reward bonuses = wire signup + referral only, DROP birthday (no birthdate captured) + remove its admin knob/marketing. **Tracked as tasks #43–58.** DONE + on dev: #43 mobile COMPLETED/ABSENT statuses (was a crash), #47 web static-QR advance subtracts points, #56 setBackgroundMessageHandler + appVersion on registration (batch `bc2b229`); #45 mobile gateway tile→"Pay Online", #57 MSG91 log redaction + web bowling empty-state admin-path leak + points-expiry caption de-hardcoded + delete dead AdvancePaymentPicker.tsx (batch `ac6754c`); #58 verified (CI release lanes already call configure_ota("production") — no fix needed). **More DONE + on dev:** #46 server-side payment-method config enforcement via new `isCheckoutMethodEnabled()` guarding placeCustomerOrder (shop, web+mobile) + createCafeOrder (web cafe) + mobile cafe route (`dbcbe94`); #50 late-night 12am–1am storage-coords — recurring uses resolvedLockDate/Hours + dayOfWeek; waitlist join (web dialog + mobile sheet) gets new lockDate/lockHour props for the API while date/hour stay display-only (`28b192a`); #48 mobile name-capture — OtpScreen is now 2-step (OTP→name) for users without a name, reusing authApi.updateName (`eef9fe3`). #49 mobile cancel recurring series — new POST /api/mobile/recurring/cancel (bearer auth, replicates web cancelRecurringBooking txn) + bookingsApi.cancelRecurring + "Cancel series" button on each RecurringCard (`2106059`). **REMAINING (6, all larger builds):** #44 wire signup+referral bonuses (drop birthday), #51 in-app account deletion (web+mobile+server, Apple req — may add schema), #52 mobile cafe parity (DQR+coupon+guest+note), #53 DQR Save-QR(mobile)+expiry countdown(both), #54 mobile coupons browse screen, #55 mobile calendar/ICS export. (Bowling time-label format left as-is — cosmetic.) Batches on dev so far: bc2b229, ac6754c, dbcbe94, 28b192a, eef9fe3, 2106059 — single main promotion after all 6 + dev spot-check. NOTE: if #51/#44 add Prisma columns, do NOT [skip ci] on the main merge (seed-production's db push must create them on prod).

**Still outstanding (older carry-over):** Android push UNTESTED — user has no Android device (an Android internal build would validate it without the Apple chain). Rotate the chat-leaked STAGING_DB_URL + BLOB_READ_WRITE_TOKEN. Play go-live (store listing + content rating) for production launch. iOS production OTA canary only activates once a production native build exists (creates the prod baseline).
